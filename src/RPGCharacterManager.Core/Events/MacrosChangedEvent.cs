namespace RPGCharacterManager.Core.Events;

/// <summary>
/// Состав макросов изменился: макрос создан, изменён или удалён.
///
/// Событие нужно горячим клавишам: сочетания принадлежат макросам, а те живут
/// в базе данных и правятся во время работы приложения. Без события главное окно
/// узнавало бы о новом сочетании только при следующем запуске.
/// </summary>
public sealed record MacrosChangedEvent;
